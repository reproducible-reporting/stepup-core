root:
             creates   file:plan.py
             creates   step:./plan.py

file:plan.py
               state = STATIC
          created by   root:
            supplies   step:./plan.py

step:./plan.py
               state = SUCCEEDED
                need = PLAN
             env_var = STEPUP_PATH_FILTER [amended]
          created by   root:
            consumes   file:plan.py
             creates   file:sub/plan.py
             creates   step:./plan.py  # wd=sub
             creates   step:echo hello > out.txt

file:sub/plan.py
               state = STATIC
          created by   step:./plan.py
            supplies   step:./plan.py  # wd=sub

step:echo hello > out.txt
               state = SUCCEEDED
                need = DEFAULT (implied by consumers > OPTIONAL)
          created by   step:./plan.py
             creates   file:out.txt
            supplies   file:out.txt

file:out.txt
               state = BUILT
          created by   step:echo hello > out.txt
            consumes   step:echo hello > out.txt
            supplies   step:cp -p ../out.txt final.txt  # wd=sub

step:./plan.py  # wd=sub
               state = SUCCEEDED
                need = PLAN
             env_var = STEPUP_PATH_FILTER [amended]
          created by   step:./plan.py
            consumes   file:sub/plan.py
             creates   step:cp -p ../out.txt final.txt  # wd=sub

step:cp -p ../out.txt final.txt  # wd=sub
               state = SUCCEEDED
                need = DEFAULT
          created by   step:./plan.py  # wd=sub
            consumes   file:out.txt
             creates   file:sub/final.txt
            supplies   file:sub/final.txt

file:sub/final.txt
               state = BUILT
          created by   step:cp -p ../out.txt final.txt  # wd=sub
            consumes   step:cp -p ../out.txt final.txt  # wd=sub
