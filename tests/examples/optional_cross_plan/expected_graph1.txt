root:
             product   file:plan.py
             product   step:./plan.py

file:plan.py
               state = STATIC
             creator   root:
                sink   step:./plan.py

step:./plan.py
               state = SUCCEEDED
                need = PLAN
           using_env = STEPUP_PATH_FILTER [dynamic]
             creator   root:
              source   file:plan.py
             product   file:sub/plan.py
             product   step:./plan.py  # wd=sub
             product   step:echo hello > out.txt

file:sub/plan.py
               state = STATIC
             creator   step:./plan.py
                sink   step:./plan.py  # wd=sub

step:echo hello > out.txt
               state = SUCCEEDED
                need = DEFAULT (implied by sinks > OPTIONAL)
             creator   step:./plan.py
             product   file:out.txt
                sink   file:out.txt

file:out.txt
               state = BUILT
             creator   step:echo hello > out.txt
              source   step:echo hello > out.txt
                sink   step:cp -p ../out.txt final.txt  # wd=sub

step:./plan.py  # wd=sub
               state = SUCCEEDED
                need = PLAN
           using_env = STEPUP_PATH_FILTER [dynamic]
             creator   step:./plan.py
              source   file:sub/plan.py
             product   step:cp -p ../out.txt final.txt  # wd=sub

step:cp -p ../out.txt final.txt  # wd=sub
               state = SUCCEEDED
                need = DEFAULT
             creator   step:./plan.py  # wd=sub
              source   file:out.txt
             product   file:sub/final.txt
                sink   file:sub/final.txt

file:sub/final.txt
               state = BUILT
             creator   step:cp -p ../out.txt final.txt  # wd=sub
              source   step:cp -p ../out.txt final.txt  # wd=sub
