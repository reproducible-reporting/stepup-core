root:
             creates   file:plan.py
             creates   step:./plan.py

file:plan.py
               state = STATIC
          created by   root:
            supplies   step:./plan.py

step:./plan.py
               state = SUCCEEDED
                need = PLAN
           using_env = STEPUP_PATH_FILTER [amended]
          created by   root:
            consumes   file:plan.py
             creates   file:README.txt
             creates   file:inp1.txt
             creates   file:sub/getinfo.py
             creates   file:sub/inp0.txt
             creates   step:./getinfo.py  # wd=sub

file:README.txt
               state = STATIC
          created by   step:./plan.py
            supplies   step:./getinfo.py  # wd=sub

file:inp1.txt
               state = STATIC
          created by   step:./plan.py
            supplies   step:./getinfo.py  # wd=sub

file:sub/getinfo.py
               state = STATIC
          created by   step:./plan.py
            supplies   step:./getinfo.py  # wd=sub

file:sub/inp0.txt
               state = STATIC
          created by   step:./plan.py
            supplies   step:./getinfo.py  # wd=sub

step:./getinfo.py  # wd=sub
               state = SUCCEEDED
                need = DEFAULT
           using_env = BAR
                     = EGG [amended]
                     = FOO
                     = STEPUP_PATH_FILTER [amended]
          created by   step:./plan.py
            consumes   file:README.txt
            consumes   file:inp1.txt [amended]
            consumes   file:sub/getinfo.py
            consumes   file:sub/inp0.txt
             creates   file:out0.txt
             creates   file:out2.txt
             creates   file:sub/out1.txt
             creates   file:sub/vol0.txt
             creates   file:vol1.txt
             creates   file:vol2.txt
            supplies   file:out0.txt
            supplies   file:out2.txt [amended]
            supplies   file:sub/out1.txt
            supplies   file:sub/vol0.txt
            supplies   file:vol1.txt
            supplies   file:vol2.txt [amended]

file:out0.txt
               state = BUILT
          created by   step:./getinfo.py  # wd=sub
            consumes   step:./getinfo.py  # wd=sub

file:sub/out1.txt
               state = BUILT
          created by   step:./getinfo.py  # wd=sub
            consumes   step:./getinfo.py  # wd=sub

file:sub/vol0.txt
               state = VOLATILE
          created by   step:./getinfo.py  # wd=sub
            consumes   step:./getinfo.py  # wd=sub

file:vol1.txt
               state = VOLATILE
          created by   step:./getinfo.py  # wd=sub
            consumes   step:./getinfo.py  # wd=sub

file:out2.txt
               state = BUILT
          created by   step:./getinfo.py  # wd=sub
            consumes   step:./getinfo.py  # wd=sub

file:vol2.txt
               state = VOLATILE
          created by   step:./getinfo.py  # wd=sub
            consumes   step:./getinfo.py  # wd=sub
