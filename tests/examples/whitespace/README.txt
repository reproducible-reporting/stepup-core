This example verifies that StepUp correctly handles file paths that contain spaces,
both in static source files and in step inputs and outputs.

The plan uses three different step types — copy, call, and script — each of which
reads or writes files whose names contain spaces.
