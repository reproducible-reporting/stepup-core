root:
             creates   file:plan.py
             creates   step:runpy ./plan.py

file:plan.py
               state = STATIC
          created by   root:
            supplies   step:runpy ./plan.py

step:runpy ./plan.py
               state = SUCCEEDED
                need = PLAN
             env_var = ENV_VAR_TEST_STEPUP_INP [amended]
                     = STEPUP_PATH_FILTER [amended]
          created by   root:
            consumes   file:plan.py
             creates   sr:static/
             creates   step:copy static/sub/bar.txt copy.txt

file:copy.txt
               state = BUILT
          created by   step:copy static/sub/bar.txt copy.txt
            consumes   step:copy static/sub/bar.txt copy.txt

sr:static/
          created by   step:runpy ./plan.py
             creates   file:static/sub/bar.txt

step:copy static/sub/bar.txt copy.txt
               state = SUCCEEDED
                need = DEFAULT
          created by   step:runpy ./plan.py
            consumes   file:static/sub/bar.txt
             creates   file:copy.txt
            supplies   file:copy.txt

file:static/sub/bar.txt
               state = STATIC
          created by   sr:static/
            supplies   step:copy static/sub/bar.txt copy.txt
