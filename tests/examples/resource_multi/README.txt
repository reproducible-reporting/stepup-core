Three steps each requiring different combinations of two resource types (slot and token).
With slot:1 and token:2 available, at most two steps can run concurrently:
one holding the slot and one holding a token, or two holding only a token each.
All steps must eventually succeed.
