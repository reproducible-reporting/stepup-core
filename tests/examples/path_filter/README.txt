This example tests (changes in) the STEPUP_PATH_FILTER variable.
It directly uses the function `filter_dependencies` to keep things simple.
In realistic scenario's this function is not directly called from a `plan.py` script.
