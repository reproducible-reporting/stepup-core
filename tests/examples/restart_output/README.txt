When StepUp is restarted, it scans all files in the database for changes.
When inputs to a step have not changed and outputs are also intact, the step is not queued.

This example shows a restart with no changes in the input, but the output has been removed.
