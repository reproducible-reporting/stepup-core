The directory of a volatile output is created before the step runs, just like the directory
of a regular output. When the step disappears from the workflow, the volatile file is
removed and its directory with it, because nothing else is left in it.
