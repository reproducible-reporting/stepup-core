root:
             creates   file:plan.py
             creates   step:./plan.py

file:plan.py
               state = STATIC
          created by   root:
            supplies   step:./plan.py

step:./plan.py
               state = SUCCEEDED
                need = PLAN
             env_var = STEPUP_PATH_FILTER [amended]
          created by   root:
            consumes   file:plan.py
             creates   file:bar/plan.py
             creates   file:foo/inp.txt
             creates   file:inp.txt
             creates   step:./plan.py  # wd=bar/

file:bar/plan.py
               state = STATIC
          created by   step:./plan.py
            supplies   step:./plan.py  # wd=bar/

file:foo/inp.txt
               state = STATIC
          created by   step:./plan.py
            supplies   step:cp -p ../foo/inp.txt ../foo/out.txt  # wd=bar/

file:inp.txt
               state = STATIC
          created by   step:./plan.py
            supplies   step:cp -p ../inp.txt ../out.txt  # wd=bar/

step:./plan.py  # wd=bar/
               state = SUCCEEDED
                need = PLAN
             env_var = INPS [amended]
                     = STEPUP_PATH_FILTER [amended]
          created by   step:./plan.py
            consumes   file:bar/plan.py
             creates   step:cp -p ../foo/inp.txt ../foo/out.txt  # wd=bar/
             creates   step:cp -p ../inp.txt ../out.txt  # wd=bar/

step:cp -p ../inp.txt ../out.txt  # wd=bar/
               state = SUCCEEDED
                need = DEFAULT
          created by   step:./plan.py  # wd=bar/
            consumes   file:inp.txt
             creates   file:out.txt
            supplies   file:out.txt

file:out.txt
               state = BUILT
          created by   step:cp -p ../inp.txt ../out.txt  # wd=bar/
            consumes   step:cp -p ../inp.txt ../out.txt  # wd=bar/

step:cp -p ../foo/inp.txt ../foo/out.txt  # wd=bar/
               state = SUCCEEDED
                need = DEFAULT
          created by   step:./plan.py  # wd=bar/
            consumes   file:foo/inp.txt
             creates   file:foo/out.txt
            supplies   file:foo/out.txt

file:foo/out.txt
               state = BUILT
          created by   step:cp -p ../foo/inp.txt ../foo/out.txt  # wd=bar/
            consumes   step:cp -p ../foo/inp.txt ../foo/out.txt  # wd=bar/
