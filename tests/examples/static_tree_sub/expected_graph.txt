root:
             creates   file:plan.py
             creates   step:./plan.py

file:plan.py
               state = STATIC
          created by   root:
            supplies   step:./plan.py

step:./plan.py
               state = SUCCEEDED
                need = PLAN
           using_env = STEPUP_PATH_FILTER [amended]
          created by   root:
            consumes   file:plan.py
             creates   file:sub/plan.py
             creates   step:./plan.py  # wd=sub

file:sub/plan.py
               state = STATIC
          created by   step:./plan.py
            supplies   step:./plan.py  # wd=sub

step:./plan.py  # wd=sub
               state = SUCCEEDED
                need = PLAN
           using_env = STEPUP_PATH_FILTER [amended]
          created by   step:./plan.py
            consumes   file:sub/plan.py
             creates   st:sub/data/
             creates   step:cp -p data/used.txt copy.txt  # wd=sub

st:sub/data/
          created by   step:./plan.py  # wd=sub
             creates   file:sub/data/used.txt

step:cp -p data/used.txt copy.txt  # wd=sub
               state = SUCCEEDED
                need = DEFAULT
          created by   step:./plan.py  # wd=sub
            consumes   file:sub/data/used.txt
             creates   file:sub/copy.txt
            supplies   file:sub/copy.txt

file:sub/data/used.txt
               state = STATIC
          created by   st:sub/data/
            supplies   step:cp -p data/used.txt copy.txt  # wd=sub

file:sub/copy.txt
               state = BUILT
          created by   step:cp -p data/used.txt copy.txt  # wd=sub
            consumes   step:cp -p data/used.txt copy.txt  # wd=sub
