root:
             product   file:plan.py
             product   step:./plan.py

file:plan.py
               state = CONFIRMED
             creator   root:
                sink   step:./plan.py

step:./plan.py
               state = SUCCEEDED
                need = PLAN
           using_env = STEPUP_PATH_FILTER [dynamic]
             creator   root:
              source   file:plan.py
             product   step:cat out.txt
             product   step:echo out > out.txt; echo vol > vol.log

step:echo out > out.txt; echo vol > vol.log
               state = SUCCEEDED
                need = DEFAULT (implied by sinks > OPTIONAL)
             creator   step:./plan.py
             product   file:out.txt
             product   file:vol.log
                sink   file:out.txt
                sink   file:vol.log

file:out.txt
               state = BUILT
             creator   step:echo out > out.txt; echo vol > vol.log
              source   step:echo out > out.txt; echo vol > vol.log
                sink   step:cat out.txt

file:vol.log
               state = VOLATILE
             creator   step:echo out > out.txt; echo vol > vol.log
              source   step:echo out > out.txt; echo vol > vol.log

step:cat out.txt
               state = SUCCEEDED
                need = DEFAULT
             creator   step:./plan.py
              source   file:out.txt
