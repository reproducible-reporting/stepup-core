A named glob with two patterns only matches when both halves are present. Adding the
missing half of a pair (tail_y) while StepUp is watching creates a new match (paste_y),
while a still-incomplete pair (head_z) is ignored.
