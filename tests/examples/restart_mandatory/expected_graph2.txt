root:
             creates   file:plan.py
             creates   step:./plan.py

file:plan.py
               state = STATIC
          created by   root:
            supplies   step:./plan.py

step:./plan.py
               state = SUCCEEDED
                need = PLAN
           using_env = STEPUP_PATH_FILTER [amended]
           explained = yes
          created by   root:
            consumes   file:plan.py
             creates   step:cp -p foo.txt bar.txt
             creates   step:echo hello > foo.txt

step:echo hello > foo.txt
               state = SUCCEEDED
                need = DEFAULT (implied by consumers > OPTIONAL)
           explained = yes
          created by   step:./plan.py
             creates   file:foo.txt
            supplies   file:foo.txt

file:foo.txt
               state = BUILT
          created by   step:echo hello > foo.txt
            consumes   step:echo hello > foo.txt
            supplies   step:cp -p foo.txt bar.txt

step:cp -p foo.txt bar.txt
               state = SUCCEEDED
                need = DEFAULT
           explained = yes
          created by   step:./plan.py
            consumes   file:foo.txt
             creates   file:bar.txt
            supplies   file:bar.txt

file:bar.txt
               state = BUILT
          created by   step:cp -p foo.txt bar.txt
            consumes   step:cp -p foo.txt bar.txt
