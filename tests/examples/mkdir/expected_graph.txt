root:
             creates   file:./
             creates   file:plan.py
             creates   step:stepup.core.actions.runpy ./plan.py

file:./
               state = STATIC
          created by   root:
            supplies   file:exists/
            supplies   file:plan.py
            supplies   file:sub/
            supplies   step:stepup.core.actions.mkdir exists/
            supplies   step:stepup.core.actions.mkdir sub/
            supplies   step:stepup.core.actions.runpy ./plan.py
            supplies   step:stepup.core.actions.runsh echo a > sub/foo.txt

file:plan.py
               state = STATIC
          created by   root:
            consumes   file:./
            supplies   step:stepup.core.actions.runpy ./plan.py

step:stepup.core.actions.runpy ./plan.py
               state = SUCCEEDED
          created by   root:
            consumes   file:./
            consumes   file:plan.py
             creates   step:stepup.core.actions.mkdir exists/
             creates   step:stepup.core.actions.mkdir sub/
             creates   step:stepup.core.actions.runsh echo a > sub/foo.txt

step:stepup.core.actions.mkdir sub/
               state = SUCCEEDED
          created by   step:stepup.core.actions.runpy ./plan.py
            consumes   file:./
             creates   file:sub/
            supplies   file:sub/

file:sub/
               state = BUILT
          created by   step:stepup.core.actions.mkdir sub/
            consumes   file:./
            consumes   step:stepup.core.actions.mkdir sub/
            supplies   file:sub/foo.txt
            supplies   step:stepup.core.actions.runsh echo a > sub/foo.txt

step:stepup.core.actions.runsh echo a > sub/foo.txt
               state = SUCCEEDED
          created by   step:stepup.core.actions.runpy ./plan.py
            consumes   file:./
            consumes   file:sub/
             creates   file:sub/foo.txt
            supplies   file:sub/foo.txt

file:sub/foo.txt
               state = BUILT
          created by   step:stepup.core.actions.runsh echo a > sub/foo.txt
            consumes   file:sub/
            consumes   step:stepup.core.actions.runsh echo a > sub/foo.txt

step:stepup.core.actions.mkdir exists/
               state = SUCCEEDED
          created by   step:stepup.core.actions.runpy ./plan.py
            consumes   file:./
             creates   file:exists/
            supplies   file:exists/

file:exists/
               state = BUILT
          created by   step:stepup.core.actions.mkdir exists/
            consumes   file:./
            consumes   step:stepup.core.actions.mkdir exists/
