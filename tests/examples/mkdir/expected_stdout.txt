   STARTUP │ (Re)initialized boot script
  DIRECTOR │ Launched worker 0
     PHASE │ run
     START │ ./plan.py
   SUCCESS │ ./plan.py
     START │ mkdir -p sub/
   SUCCESS │ mkdir -p sub/
     START │ mkdir -p exists/
   SUCCESS │ mkdir -p exists/
     START │ echo a > sub/foo.txt
   SUCCESS │ echo a > sub/foo.txt
  DIRECTOR │ Trying to delete 0 outdated output(s).
     PHASE │ watch
  DIRECTOR │ Stopping workers.
  DIRECTOR │ See you!
