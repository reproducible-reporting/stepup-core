Parent directories are automatically added as dependencies of outputs.
This implies that nested directories must be made step by step.
This example shows the error message when trying to make a set of nested directories at once.
